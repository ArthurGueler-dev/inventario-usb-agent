[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "in9-usb-agent"
version = "1.0.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["agent*"]
